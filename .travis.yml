language: java

dist: xenial

services:
  - docker

jdk:
  - openjdk11
before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar
script:
  - docker-compose build
  - docker-compose up
  - ./gradlew check
